const onFieldChange = (key, value, updateProps) => updateProps({ [key]: value });

const mock = () => {};

const updateProps = (state, { payload }) => {};

export { mock, onFieldChange, updateProps };
